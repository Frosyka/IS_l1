% Понятия
сущность(мука).
сущность(яйца).
сущность(мясо).
сущность(картофель).
сущность(рис).
сущность(овощи).
сущность(сыр).
сущность(духовка).
сущность(блины).
сущность(жареное_мясо_с_картошкой).
сущность(овощной_плов).
сущность(горячие_бутерброды).

% Отношения
является_ингредиентом(мука, блины).
является_ингредиентом(яйца, блины).
является_ингредиентом(мясо, жареное_мясо_с_картошкой).
является_ингредиентом(картофель, жареное_мясо_с_картошкой).
является_ингредиентом(рис, овощной_плов).
является_ингредиентом(овощи, овощной_плов).
является_ингредиентом(сыр, горячие_бутерброды).

приготовлено_с_использованием(духовка, горячие_бутерброды).

является_типом(блины, выпечка).
является_типом(жареное_мясо_с_картошкой, мясные_блюда).
является_типом(овощной_плов, вегетарианские_блюда).
является_типом(горячие_бутерброды, закуски).

% Запросы
ингредиенты_для(Блюдо, Ингредиент) :- является_ингредиентом(Ингредиент, Блюдо).
оборудование_для(Блюдо, Оборудование) :- приготовлено_с_использованием(Оборудование, Блюдо).
тип_блюда(Блюдо, Тип) :- является_типом(Блюдо, Тип).
